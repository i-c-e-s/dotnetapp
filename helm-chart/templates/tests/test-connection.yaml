apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sky-act-core.fullname" . }}-test-connection"
  labels:
    {{- include "sky-act-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sky-act-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
